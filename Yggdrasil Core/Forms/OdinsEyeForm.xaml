<UserControl x:Class="Yggdrasil_Core.Forms.OdinsEyeForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:vm="clr-namespace:Yggdrasil_Core.ViewModels"
             mc:Ignorable="d" d:DesignWidth="530" Background="#0A0A0A" Height="430">
    <UserControl.DataContext>
        <vm:OdinsEyeVM />
    </UserControl.DataContext>
    <UserControl.Resources>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#202020" />
        <SolidColorBrush x:Key="BorderBrush" Color="#333333" />
        <Style x:Key="ToggleSwitchStyle" TargetType="ToggleButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid VerticalAlignment="Center">
                            <Border x:Name="Border" Width="30" Height="6" Background="#FF843333" CornerRadius="3" VerticalAlignment="Center" />
                            <Ellipse x:Name="Thumb" Width="12" Height="12" Fill="#FF1D1D1D" HorizontalAlignment="Left" Margin="0">
                                <Ellipse.RenderTransform>
                                    <TranslateTransform x:Name="ThumbTransform" X="0"/>
                                </Ellipse.RenderTransform>
                            </Ellipse>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ThumbTransform" Storyboard.TargetProperty="X" To="18" Duration="0:0:0.2"/>
                                            <ColorAnimation Storyboard.TargetName="Border" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="LimeGreen" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ThumbTransform" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.2"/>
                                            <ColorAnimation Storyboard.TargetName="Border" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#FF843333" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <StackPanel Orientation="Horizontal">
            <StackPanel Width="150" Margin="0,0,10,0">
                <Border Background="{StaticResource SecondaryBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Box Preview" Foreground="#DCDCDC" FontWeight="Bold" Margin="5,3,4,5" />
                        <Border Height="131" Background="Transparent" Margin="5,0,4,5" >
                            <Grid>
                                <Rectangle x:Name="previewRect" HorizontalAlignment="Center" Height="{Binding BoxSize}" Width="{Binding BoxSize}" Stroke="Black" VerticalAlignment="Top" Margin="0,10,0,0" StrokeThickness="{Binding BoxThickness}" Opacity="{Binding BoxOpacity}" />
                                <TextBlock x:Name="boxTextPreview" HorizontalAlignment="Center" Margin="0,45,0,0" TextWrapping="Wrap" VerticalAlignment="Top" FontSize="8" Foreground="#DCDCDC" />
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>
                <Border Background="{StaticResource SecondaryBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Height="258">
                    <StackPanel Height="245">
                        <TextBlock Text="Entities" Foreground="#DCDCDC" FontWeight="Bold" Margin="5,0,4,5" />
                        <CheckBox Content="Player" IsChecked="{Binding ShowPlayer}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                        <CheckBox Content="Friend" IsChecked="{Binding ShowFriend}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                        <CheckBox Content="Party" IsChecked="{Binding ShowParty}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                        <CheckBox Content="Guild" IsChecked="{Binding ShowGuild}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                        <CheckBox Content="Monsters" IsChecked="{Binding ShowMonsters}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                        <CheckBox Content="Boss" IsChecked="{Binding ShowBoss}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                        <CheckBox Content="Minions" IsChecked="{Binding ShowMinions}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                        <CheckBox Content="Items" IsChecked="{Binding ShowItem}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                        <CheckBox Content="NPC" IsChecked="{Binding ShowNPC}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                    </StackPanel>
                </Border>
            </StackPanel>
            <StackPanel Width="370">
                <Border Background="{StaticResource SecondaryBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Box Settings" Foreground="#DCDCDC" FontWeight="Bold" Margin="5,0,4,5" HorizontalAlignment="Left" />
                        <Grid Height="93">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,5,-10">
                                <StackPanel Orientation="Horizontal" Margin="5,0,4,5" >
                                    <TextBlock Text="Size" Width="80" VerticalAlignment="Center" Foreground="#DCDCDC" />
                                    <Slider Value="{Binding BoxSize}" Minimum="30" Maximum="100" Width="150" Height="19" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="5,0,4,5" >
                                    <TextBlock Text="Thickness" Width="80" VerticalAlignment="Center" Foreground="#DCDCDC" />
                                    <Slider Value="{Binding BoxThickness}" Minimum="0.5" Maximum="3" Width="150" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="5,0,4,5" >
                                    <TextBlock Text="Width" Width="80" VerticalAlignment="Center" Foreground="#DCDCDC" />
                                    <Slider Value="{Binding BoxWidth}" Minimum="0.5" Maximum="1" Width="150" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="5,0,4,5" >
                                    <TextBlock Text="Opacity" Width="80" VerticalAlignment="Center" Foreground="#DCDCDC" />
                                    <Slider Value="{Binding BoxOpacity}" Minimum="0.1" Maximum="1" Width="150" />
                                </StackPanel>
                            </StackPanel>
                            <hc:ColorPicker Margin="0,0,10,10" HorizontalAlignment="Right" Width="118" Height="83" VerticalAlignment="Bottom" Background="#FF823939" />
                        </Grid>
                    </StackPanel>
                </Border>
                <Border Background="{StaticResource SecondaryBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="Entity Info Settings" Foreground="#DCDCDC" FontWeight="Bold" Margin="5,0,4,5" HorizontalAlignment="Left" />
                        <Grid Height="60" Margin="0,0,0,1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                <CheckBox Content="Line" IsChecked="{Binding ShowLine}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                                <CheckBox Content="ID" IsChecked="{Binding ShowID}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                                <CheckBox Content="Name" IsChecked="{Binding ShowName}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <CheckBox Content="Coordinates" IsChecked="{Binding ShowCoordinate}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                                <CheckBox Content="HP Bar" IsChecked="{Binding ShowHPBar}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                                <CheckBox Content="SP Bar" IsChecked="{Binding ShowSPBar}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
                <Border Background="{StaticResource SecondaryBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Margin="0,0,0,10">
                    <StackPanel>
                        <DockPanel Width="369" HorizontalAlignment="Left">
                            <TextBlock Text="Job Filter" DockPanel.Dock="Left" Width="314" Margin="5,0,0,5" Foreground="White" FontWeight="Bold" />
                            <ToggleButton x:Name="jobFilter" Style="{StaticResource ToggleSwitchStyle}" IsChecked="False" Width="30" Cursor="Hand" />
                        </DockPanel>
                        <Grid Height="144" Margin="0,0,0,1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,5,0">
                                <CheckBox Content="Lord Knight" IsChecked="{Binding ShowLordKnight}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                                <CheckBox Content="High Priest" IsChecked="{Binding ShowHighPriest}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                                <CheckBox Content="High Wizard" IsChecked="{Binding ShowHighWizard}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                                <CheckBox Content="Whitesmith" IsChecked="{Binding ShowWhitesmith}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                                <CheckBox Content="Sniper" IsChecked="{Binding ShowSniper}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                                <CheckBox Content="Minstrel" IsChecked="{Binding ShowMinstrel}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                                <CheckBox Content="Super Novice" IsChecked="{Binding ShowSuperNovice}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <CheckBox Content="Assassin Cross" IsChecked="{Binding ShowAssassinCross}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                                <CheckBox Content="Paladin" IsChecked="{Binding ShowPaladin}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                                <CheckBox Content="Champion" IsChecked="{Binding ShowChampion}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                                <CheckBox Content="Professor" IsChecked="{Binding ShowProfessor}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                                <CheckBox Content="Stalker" IsChecked="{Binding ShowStalker}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                                <CheckBox Content="Biochemist/Creator" IsChecked="{Binding ShowBiochemist}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                                <CheckBox Content="Novice" IsChecked="{Binding ShowNovice}" Margin="5,0,4,5" Foreground="#DCDCDC" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
                <Border Background="{StaticResource SecondaryBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Height="32">
                    <StackPanel HorizontalAlignment="Center" Margin="0,0,0,-1">
                        <DockPanel Width="369" HorizontalAlignment="Left" Height="31">
                            <TextBlock Text="Maya Purple" DockPanel.Dock="Left" Width="314" Margin="5,0,0,0" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center" />
                            <ToggleButton x:Name="mayaPurpleToggleBtn" Style="{StaticResource ToggleSwitchStyle}" IsChecked="False" Width="30" Cursor="Hand" Height="16" />
                        </DockPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>